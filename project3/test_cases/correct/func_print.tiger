main let
    var X : float := 0.0;
    var Y : int;
    function myPrint (X : int) begin
        printi(X);      /* print int */
    end
in begin
    X := readf();
    printf(X + 0.001);      /* print float */
    Y := readi();
    myPrint(1 - Y + 3 * Y);
end
