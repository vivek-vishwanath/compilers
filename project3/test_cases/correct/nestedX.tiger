main let 
    var X : int;
in begin
    X := readi();
    printi(X);
    printc(32);  /* SPACE */
    
    let 
        var X : int;
    in
        X := readi();
        printi(X);
        printc(32);  /* SPACE */

        let
            var X : float;
        in
            X := readf();
            printf(X);
            printc(32);  /* SPACE */
        end

        printi(X);
        printc(32);   /* SPACE */
    end
    
    printi(X);
    printc(32);   /* SPACE */
end