main let
    var a: int := 12;
    var b: int := 3;
    var c: int;
    var d: int := 0;
    var e: int;
    var f: int;
    var g: int := 1;
    var h: int := 1;
    var i: int := 10;
    var j: int := 7;
    var k: int := 12;
    var l: int := 3;
    var m: int := 2;
in begin
    c := 0 - 1;
    e := 0 - 2;
    f := 0 - 1;
    let
        var x: int;
    in
        x := (a | b) & c <= d >= e < f > g <> h = i - j + k / l * m;
        printi(x);
    end
    printc(10);
end
